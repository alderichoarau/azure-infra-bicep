# Editors
.vscode/
.idea/
*.swp

# Bicep / ARM — compiled JSON output (built on the fly locally/in CI, never committed)
*.bicep.json
main.json
**/*.compiled.json

# Local override parameter files — never commit real SSH keys/IPs
*.local.json
*.local.parameters.json

# What-if / deployment output
whatif-*.txt
deployment-*.json

# OS
.DS_Store
Thumbs.db
